#include "main.h"
/**
 * _isdigit-a function that checjks for a digit
 * @c:value of ascii
 * Return:1 success 0 otherwise
 */
int _isdigit(int c)
{
	int a;

	if (a >= 48 && a <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
