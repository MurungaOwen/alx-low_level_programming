#include<stdio.h>
/**
 * main-a function that calls itself
 *@argc:the number of argument
 *@*argv[]:the array of values in v
 * return:0 success
 */
int main(int argc ,char *argv[])
{

	printf("%s",*argv[0]);
	printf("\n");
	return (0)
}
