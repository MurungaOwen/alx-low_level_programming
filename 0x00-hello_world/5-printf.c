#include<stdio.h>
/**
 * main-a program that uses printf to display characters
 * description-using printf instead of puts
 * Return:0 (success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
